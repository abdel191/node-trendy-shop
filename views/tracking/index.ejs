<!DOCTYPE html>
<html lang="fr">
  <head>

    <meta charset="UTF-8" />
    <title>Suivi de commande - <%= order.trackingCode %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
   <%- include("../partials/header") %>
  <body>
    <div
      class="container"
      style="
        max-width: 800px;
        margin: 50px auto;
        font-family: Arial, sans-serif;
      "
    >
      <h1>📦 Suivi de votre commande</h1>
      <p>Commande <b><%= order.trackingCode %></b></p>

      <div
        class="order-status"
        style="
          margin: 20px 0;
          padding: 15px;
          border-radius: 8px;
          border: 1px solid #ddd;
        "
      >
        <h2>Statut :</h2>
        <p><b><%= order.status %></b></p>
        <p>
          Date estimée de livraison :
          <b><%= order.estimatedDelivery.toLocaleDateString() %></b>
        </p>
      </div>

      <div
        class="customer-info"
        style="
          margin: 20px 0;
          padding: 15px;
          border-radius: 8px;
          border: 1px solid #ddd;
        "
      >
        <h2>Informations client :</h2>
        <p><b>Nom :</b> <%= order.customerName %></p>
        <p><b>Email :</b> <%= order.customerEmail %></p>
        <p><b>Adresse :</b> <%= order.address %></p>
      </div>

      <div
        class="order-items"
        style="
          margin: 20px 0;
          padding: 15px;
          border-radius: 8px;
          border: 1px solid #ddd;
        "
      >
        <h2>Détails des produits :</h2>
        <ul>
          <% order.orderItems.forEach(item => { %>
          <li>
            <%= item.quantity %> ×
            <span><%= item.price %> €</span>
            <span style="color: gray"
              >(Produit ID: <%= item.productId || 'N/A' %>)</span
            >
          </li>
          <% }) %>
        </ul>
        <p><b>Total : <%= order.totalAmount %> €</b></p>
      </div>

      <a
        href="/"
        style="
          display: inline-block;
          margin-top: 20px;
          padding: 12px 20px;
          background: #007bff;
          color: white;
          text-decoration: none;
          border-radius: 6px;
        "
      >
        ⬅️ Retour à la boutique
      </a>
    </div>
  </body>
   <%- include("../partials/footer") %>
</html>

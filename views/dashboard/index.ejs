<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Mon compte – TrendyShop</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <%- include("../partials/header") %>

    <div class="container mx-auto px-4 py-10">
      <h1 class="text-3xl font-bold mb-6">Bonjour <%= user.name %> 👋</h1>

      <!-- INFOS UTILISATEUR -->
      <div class="bg-white p-6 rounded shadow mb-8">
        <p><strong>Email :</strong> <%= user.email %></p>
      </div>

      <!-- ACTIONS -->
      <div class="grid md:grid-cols-3 gap-6 mb-10">
        <a href="/" class="bg-indigo-600 text-white p-6 rounded text-center">
          🛍️ Continuer mes achats
        </a>

        <a href="/cart" class="bg-green-600 text-white p-6 rounded text-center">
          🛒 Mon panier
        </a>

        <a
          href="#orders"
          class="bg-gray-800 text-white p-6 rounded text-center"
        >
          📦 Mes commandes
        </a>
      </div>

      <!-- COMMANDES -->
      <div id="orders" class="bg-white p-6 rounded shadow">
        <h2 class="text-2xl font-semibold mb-4">📦 Mes commandes</h2>

        <% if (orders.length === 0) { %>
        <p class="text-gray-500">Aucune commande pour le moment.</p>
        <% } else { %>
        <table class="w-full border-collapse">
          <thead>
            <tr class="bg-gray-200 text-left">
              <th class="p-3">Commande</th>
              <th class="p-3">Total (€)</th>
              <th class="p-3">Date</th>
              <th class="p-3">Action</th>
            </tr>
          </thead>

          <tbody>
            <% orders.forEach(order => { %>
            <tr class="border-t">
              <td class="p-3 font-semibold">#<%= order.id %></td>

              <td class="p-3"><%= order.totalAmount %> €</td>

              <td class="p-3">
                <%= new Date(order.createdAt).toLocaleDateString() %>
              </td>

              <td class="p-3">
                <a
                  href="/dashboard/orders/<%= order.id %>"
                  class="text-indigo-600 hover:underline text-sm"
                >
                  Voir détails →
                </a>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <% } %>
      </div>
    </div>
  </body>
</html>

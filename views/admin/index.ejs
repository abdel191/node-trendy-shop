<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Dashboard Admin - TrendyShop</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <%- include("../partials/header") %>

    <div class="container mx-auto px-4 py-10">
      <!-- TITRE -->
      <h1 class="text-3xl font-bold mb-6">👑 Dashboard Admin</h1>

      <p class="mb-8 text-gray-700">
        Connecté en tant que <strong><%= user.name %></strong> (<%= user.email
        %>)
      </p>

      <!-- ACTIONS -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
        <a
          href="/admin/stats"
          class="bg-blue-600 hover:bg-blue-700 text-white p-6 rounded shadow text-center text-lg"
        >
          📊 Statistiques ventes
        </a>

        <a
          href="/admin/products/new"
          class="bg-indigo-600 hover:bg-indigo-700 text-white p-6 rounded shadow text-center text-lg"
        >
          ➕ Ajouter un produit
        </a>

        <a
          href="/admin/orders"
          class="bg-green-600 hover:bg-green-700 text-white p-6 rounded shadow text-center text-lg"
        >
          📦 Voir les commandes
        </a>

        <a
          href="/admin/users"
          class="bg-gray-800 hover:bg-gray-900 text-white p-6 rounded shadow text-center text-lg"
        >
          👥 Gérer les utilisateurs
        </a>
      </div>

      <!-- PRODUITS -->
      <div class="bg-white rounded shadow p-6">
        <h2 class="text-2xl font-semibold mb-4">🛍️ Produits</h2>

        <% if (products.length === 0) { %>
        <p class="text-gray-500">Aucun produit enregistré.</p>
        <% } else { %>
        <table class="w-full border-collapse">
          <thead>
            <tr class="bg-gray-200 text-left">
              <th class="p-3">Nom</th>
              <th class="p-3">Prix (€)</th>
              <th class="p-3">Actions</th>
            </tr>
          </thead>

          <tbody>
            <% products.forEach(product => { %>
            <tr class="border-t">
              <td class="p-3"><%= product.name %></td>
              <td class="p-3 font-semibold"><%= product.price %></td>
              <td class="p-3 flex gap-2">
                <a
                  href="/admin/products/<%= product.id %>/edit"
                  class="bg-yellow-500 hover:bg-yellow-600 text-white px-3 py-1 rounded text-sm"
                >
                  ✏️ Modifier
                </a>

                <form
                  action="/admin/products/<%= product.id %>/delete"
                  method="POST"
                  onsubmit="return confirm('Supprimer ce produit ?')"
                >
                  <button
                    class="bg-red-600 hover:bg-red-700 text-white px-3 py-1 rounded text-sm"
                  >
                    🗑️ Supprimer
                  </button>
                </form>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <% } %>
      </div>
    </div>
  </body>
  <%- include("../partials/footer") %>
</html>

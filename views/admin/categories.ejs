<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Catégories – Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <%- include("../partials/header") %>

    <div class="max-w-xl mx-auto mt-10 bg-white p-6 rounded shadow">
      <h1 class="text-2xl font-bold mb-4">📂 Catégories</h1>

      <form action="/admin/categories" method="POST" class="flex gap-2 mb-6">
        <input
          type="text"
          name="name"
          placeholder="Nouvelle catégorie"
          class="flex-1 border rounded px-3 py-2"
          required
        />
        <button class="bg-indigo-600 text-white px-4 rounded">Ajouter</button>
      </form>

      <ul class="space-y-2">
        <% categories.forEach(cat => { %>
        <li class="border px-3 py-2 rounded bg-gray-50"><%= cat.name %></li>
        <% }) %>
      </ul>
    </div>

    <%- include("../partials/footer") %>
  </body>
</html>
